"""Self-contained world-generation module package (relocated from backend.engine).

Added to sys.path by the module backend / core shim so its subpackages
(`worldgen`, `terrain`, `world_map`) import under the `wbworldgen.*` namespace.
"""
